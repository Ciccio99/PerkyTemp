<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ViewModels="clr-namespace:PerkyTemp.ViewModels;assembly=PerkyTemp"
             x:Class="PerkyTemp.Views.CurrentSessionPage"
             Title="Current Session">
    <ContentPage.BindingContext>
        <ViewModels:CurrentSessionViewModel x:Name="TheViewModel" />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <!-- Padding for iOS-->
            <Label Text="" />

            <Label Text="PerkyTemp" FontSize="Large" FontAttributes="Bold" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Label Text="Current temp:" FontSize="Large" Grid.Row="0" Grid.Column="0" />
                <Label Text="{Binding CurrentTemp}" FontSize="Large" Grid.Row="0" Grid.Column="1" />
            </Grid>

            <Button x:Name="Btn" Text="{Binding ButtonText}" FontSize="Large" FontAttributes="Bold" Clicked="Btn_Clicked" />
            <Label Text="{Binding Status}" FontSize="Medium" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
