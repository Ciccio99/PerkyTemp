<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ViewModels="clr-namespace:PerkyTemp.ViewModels;assembly=PerkyTemp"
             x:Class="PerkyTemp.Views.SettingsPage"
             Title="Settings">
    <ContentPage.BindingContext>
        <ViewModels:SettingsViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <StackLayout>
            <!-- Padding for iOS-->
            <Label Text="" />

            <Label Text="Settings:" FontSize="Large" FontAttributes="Bold" />

            <Label Text="Send notification when the remaining cooling time is:" FontSize="Large" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Entry FontSize="Large" Grid.Row="0" Grid.Column="0" Text="{Binding NotificationTime, Mode=TwoWay}" Keyboard="Numeric" />
                <Label Text="minutes" FontSize="Large" Grid.Row="0" Grid.Column="1" />
            </Grid>

            <Label Text="Temperature at which the vest is no longer effective:" FontSize="Large" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Entry FontSize="Large" Grid.Row="0" Grid.Column="0" Text="{Binding TemperatureThreshold, Mode=TwoWay}" Keyboard="Numeric" />
                <Label Text="deg C" FontSize="Large" Grid.Row="0" Grid.Column="1" />
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
